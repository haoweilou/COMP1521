#include <semaphore.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
 
 
int main (void)
{
	int pid;
		for (int i = 0; i < 5; i++) {
			for (int j = 0; j < 26; j++)
				putchar ('a' + j);
			putchar ('\n');
		}
	} else {
			for (int j = 0; j < 26; j++)
				putchar ('A' + j);
			putchar ('\n');
		}
	}
	return 0;
}
 
 